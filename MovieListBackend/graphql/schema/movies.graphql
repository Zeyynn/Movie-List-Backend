type Movie {
  id: ID!
  name: String!
  description: String
  release_year: Int
  rating: Float
  director: Director @belongsTo
  genre: Genre @belongsTo
}

type Query {
  movies: [Movie!]! @all
  movie(id: ID! @eq): Movie @find
}
