type Genre {
  id: ID!
  name: String!
  movies: [Movie!]! @hasMany
}

type Query {
  genres: [Genre!]! @all
  genre(id: ID! @eq): Genre @find
}

type Mutation {
  createGenre(
    name: String!
  ): Genre @create
}
